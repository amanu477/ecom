kind = "web"
previewPath = "/"
title = "FemmeFlow Store"
version = "1.0.0"
id = "artifacts/femme-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18553

[services.development]
run = "pnpm --filter @workspace/femme-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/femme-store", "run", "build" ]
publicDir = "artifacts/femme-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18553"
BASE_PATH = "/"
